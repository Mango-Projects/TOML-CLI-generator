{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Current Dir (internal)",
      "type": "python",
      "request": "launch",
      "cwd": "${fileDirname}",
      "program": "${file}",
      "console": "integratedTerminal"
    }
  ]
}